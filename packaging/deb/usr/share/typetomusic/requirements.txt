# TypeToMusic – Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework
PyQt5>=5.15.0

# Global keyboard capture (X11 + Wayland compatible)
pynput>=1.7.6

# FluidSynth Python bindings
# Requires libfluidsynth-dev to be installed on the system:
#   sudo apt install libfluidsynth-dev fluid-soundfont-gm
pyfluidsynth>=1.3.3

# Build / packaging (optional, only needed for distribution)
pyinstaller>=6.0.0
